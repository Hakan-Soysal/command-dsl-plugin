{
  "skill_name": "teknik-analiz",
  "evals": [
    {
      "id": 1,
      "name": "satinalma-teknik-tasarim",
      "prompt": "Elimde onaylanmış bir satınalma iş analizi var (CommandDSL): çalışan talep açıp gönderiyor; yöneticisi kendi ekibinin talebini mesai saatinde ve 50000 altındaysa onaylıyor; satınalmacı onaylı talebi siparişe çevirip tedarikçiye gönderiyor; depo görevlisi mal kabul yapıp siparişi kapatıyor; muhasebeci faturayı kaydedip ödüyor. Bunun teknik tasarımını (Tech DSL) çıkar: hangi bağımsız sınırlar olmalı, hangi işlem hangi sınırda, imzalar, yetkiler ve sistemler-arası tutarlılık nasıl olmalı?",
      "expected_output": "Linked .tcdsl (contract → operations.json). Birden çok module (ör. Procurement / Ordering / Receiving / Invoicing) çünkü bunlar farklı tutarlılık balonları; module-arası akış (onaylı talep → sipariş, sipariş kapanış → fatura) cross-module write taşıyan op'larda 'consistency async|durable' bildirilmeli. Her op realizes ile iş işlemine bağlı; her write-set entity kendi module'ünde realizes ediliyor (entity-kapsama). roles+ownership iş signature'ı ile uyumlu (yönetici 'managedTeam' ownership genişletilmemiş). Onay kısıtı provenance'a göre ayrışmış (ADR-0031): tutar-eşiği (50000) istek input'unda → 'validation { amount < 50000 }' (input-only); mesai-saati → takvim/policy ekseni; 'managedTeam' → ownership. guard-id link verilmiş. Gömülü validate-tech.mjs ile 0 error; çıkan warning'ler (mode-eksik vb.) kullanıcıya soru olarak yansıtılıp giderilmiş/belgelenmiş.",
      "files": []
    },
    {
      "id": 2,
      "name": "izin-talep-teknik-tek-module",
      "prompt": "Şu izin talep iş analizini teknik tasarıma dök: çalışan izin talebi gönderiyor (tarihler, sebep); yöneticisi sadece kendi ekibinin talebini onaylıyor/reddediyor; onaylanınca çalışana haber gidiyor; çalışan yönetici bakmadıysa geri çekebiliyor. Tek bir uygulama olarak dağıtılacak.",
      "expected_output": "Linked .tcdsl, tek module (tek deployable; intra-module = strong consistency → mode gerekmez). entity LeaveRequest realizes; operation'lar realizes + açık imza + access (creates/updates/reads). ownership 'managedTeam'/'own' iş signature'ıyla aynı (genişletme yok → weakening warning yok). 'yönetici bakmadıysa geri çek' = LeaveRequest'in **durumuna** (status='pending') bağlı → state-RULE; provenance gereği state-kökü bildirilmiş: 'access { reads LeaveRequest as req by id }' + 'rule { req.status = pending }' (çıplak 'status' → 'gizli veri bağımlılığı' error olurdu). guard-id link. Görünürlük her op'ta açık (@rest/@internal). 0 error.",
      "files": []
    },
    {
      "id": 3,
      "name": "eticaret-odeme-saga-idempotent",
      "prompt": "Bir e-ticaret sipariş akışının teknik tasarımını çıkar: müşteri sepeti ödeyince sipariş oluşuyor; ödeme bir dış sağlayıcıyla (ör. Stripe) yapılıyor ve başarısız olursa geri alınabilmeli; müşteri kendi siparişlerini listeliyor (liste büyüyebilir). Ödeme çağrısı ağ hatasıyla iki kez gelebilir.",
      "expected_output": "Linked .tcdsl. Dış sağlayıcı 'external Stripe { operation Charge ... }' (üretilmez, çağrı adapter'ı üretilir); ödeme op'u 'calls Stripe.Charge compensate with Stripe.Refund' (saga; 'calls' argümansız). 'idempotent by <param>' (çift çağrı no-op). Liste op'u 'paginated by cursor/offset ... size N' (list-dönen sorgu). Sipariş oluşturma cross-module/dış-state → 'consistency' + saga (cross-module WRITE = event/saga, calls ile değil); entity Order/Refund kendi module'ünde realizes. 0 error; görünürlük ve mode warning'leri giderilmiş.",
      "files": []
    },
    {
      "id": 4,
      "name": "havale-cross-module-limit-read",
      "prompt": "Bir bankacılık havale akışının teknik tasarımını çıkar: müşteri kaynak ve hedef IBAN ile havale başlatıyor; havale (1) kaynak hesabın bakiyesinin altında olmalı ve (2) ayrı bir Risk/Limit context'inden gelen günlük limiti aşmamalı; tutar 0'dan büyük olmalı; yetersiz bakiye ve limit aşımı ayrı, client'ın ayırt edebileceği hatalar. Limit bilgisi havale module'üne ait değil, Risk module'ünde tutuluyor.",
      "expected_output": "Linked .tcdsl, en az iki module (Payments + Risk; farklı tutarlılık balonları). Transfer op imzası (fromIban, toIban, amount). Provenance (ADR-0031) doğru ayrışmış: 'validation { amount > 0 }' (input-only, 400); kaynak bakiye state-rule → 'access { reads Account as src by from }' + 'rule { amount <= src.balance }' (instance-binding 'as/by'); günlük limit BAŞKA module'de → cross-module senkron read (ADR-0030): 'calls Risk.GetDailyLimit as cap' (hedef non-@internal QUERY) + 'rule { amount <= cap }'. Risk.GetDailyLimit write taşımaz (query); @internal değil. Hatalar 'error InsufficientBalance: NotProcessable' + 'error DailyLimitExceeded: NotProcessable' + op'ta 'throws'. callEdges'te Risk.GetDailyLimit → crossModule:true, consistency:eventual, kind:module (riskOf/op-{risk,mode} dokunulmaz). Çıplak 'balance'/'limit' veya 'calls Risk.GetDailyLimit(amount)' (argümanlı) YAZILMAMIŞ (ikisi de error). Gömülü validate-tech.mjs 0 error; görünürlük warning'leri giderilmiş/belgelenmiş.",
      "files": []
    },
    {
      "id": 5,
      "name": "teklif-refinement-deger-uzayi",
      "prompt": "Şu teklif (proposal) iş analizini teknik tasarıma dök: çalışan bir teklif oluşturur; teklifin bir önceliği (1 ile 5 arası bir sayı) ve bir durumu (Taslak, Gönderildi, Onaylı, Reddedildi arasından biri) vardır; oluşturma sırasında yalnız Taslak veya Gönderildi durumları geçerlidir; teklif başlığı yalnız 'rfp', 'quote' veya 'tender' türlerinden biri olabilir. Öncelik 1-5 dışında ya da tür bu üçünün dışında gelirse istek geçersiz sayılmalı (client ayırt edebilmeli).",
      "expected_output": "Linked .tcdsl. FIELD refinement: entity Proposal'da 'priority: Int in 1..5' (range — sayısal tip) ve 'status: <Enum> in {Draft|Submitted}' (union — enum ALT-KÜMESİ; enum ProposalStatus {Draft Submitted Approved Rejected} declared, dolayısıyla union-not-in-enum tetiklenmez). PARAM refinement: SubmitProposal(priority: Int in 1..5, title: String in {\"rfp\"|\"quote\"|\"tender\"}) — String skaler → permissive union geçerli. Manifest'te entities[].fields[].refinement (priority=range, status=union) VE op.violations[] = [{ruleId:'priority.range', field:'priority', domain:range}, {ruleId:'title.union', field:'title', domain:union}] görünür; violations[] = NotValid(400) hata-gövdesi sözleşmesi (taksonomi eşlemesi manifest'te ASSERT edilmez — konvansiyon tüketicide). Geçersiz biçimler YAZILMAMIŞ: sayısal-union yok, from>=to yok (STRICT), enum-dışı üye yok, negatif range yok (NUMBER işaretsiz). Param adları tekil (checkParamUniqueness). Değer-uzayı (uçlar/küme) kullanıcıdan alınmış, uydurulmamış; boundary-value (0/1/5/6, sınır dahil mi) sorulmuş. Gömülü validate-tech.mjs 0 error (yalnız kapsam warning'i); emit-manifest.mjs meta.hasErrors:false. Ref: references/examples/refinement.tcdsl.",
      "files": []
    },
    {
      "id": 6,
      "name": "adli-kural-realize-uc-predikat-sinifi",
      "prompt": "Bir kampanya/POI (point-of-interest) platformunun iş analizini teknik tasarıma dök. İş sözleşmesi (operations.json) üç ADLI iş-kuralı taşıyor (rules[] — hepsi gövdesiz, body:null, yalnız note'lu) ve operasyonlara requires ile bağlıyor: (1) PoiInActiveCampaign — 'Bir POI, aktif ya da duraklatılmış bir kampanyaya bağlıysa silinemez' (DeletePoi requires ediyor; POI-kampanya bağı ayrı bir bağlantı tablosunda, kampanyanın durumu kampanya tablosunda). (2) PoiLimitExceeded — 'Bir organizasyonun aktif POI sayısı tier limitini aşamaz' (CreatePoi requires ediyor; limit çağrı girdisinden geliyor). (3) IneligibleIndustry — 'Vergi kaydının sektörü, organizasyonun ofislerinden en az birinin sektörüyle eşleşmeli' (RegisterTaxRecord requires ediyor; sektör istek parametresi). Teknik tasarımı çıkar; kuralların teknik gerçeklemesini de yaz.",
      "expected_output": "Linked .tcdsl, tech v3.0.0 (ADR-0042). Üç predikat sınıfı da REALIZE-GÖVDESİYLE tech'te: her requires için realize-eden op'ta 'realizes rule <Ad> { … }' (TEK-AD + gövde; çoklu-ad+gövde parse-error olurdu, yazılmamış). Nicelenen HER koleksiyon önce 'access { reads <E> as <alias> [by <key>] }' ile bildirilmiş (inline entity-adı/param nicelenmemiş — T4 alias-çözümü error'u yok): (1) JOIN-EXISTS: DeletePoi'de 'reads CampaignPoi as cps by poiId' + 'reads Campaign as camps' → 'realizes rule PoiInActiveCampaign { not exists cps where exists camps where camps.id = cps.campaignId and camps.status in { active | paused } }' — iç-exists FK-eşitliği taşıyor (kartezyen-barı error'u yok), join = iç-içe exists (relation-nav yazılmamış). (2) FİLTRELİ-COUNT: CreatePoi'de 'reads Poi as orgPois by orgId' (by = ön-filtre; predikat ön-filtreyi yeniden kodlamaz) → 'realizes rule PoiLimitExceeded { count orgPois where orgPois.status = \"active\" < poiLimit }' — count TOP-LEVEL (where içinde count parse-error), sağ-taraf skaler op-yüzeyi (param). (3) PARAM-KORELASYON: RegisterTaxRecord'da 'reads Office as offices by orgId' → 'realizes rule IneligibleIndustry { exists offices where offices.industry = industry }' — çıplak kök 'industry' op-param'ına çözülür (K5 korelasyon), alias'lı kök nicelenen satıra. exists(...)/count(...) CALL-yazımı YOK (parse-error olurdu); sum/avg/min/max/all/any Call'ı YOK (deny-list error). exists/count/where/not identifier olarak KULLANILMAMIŞ (sert-rezerve). Kural sınıfı, tablolar, FK kolonları ve eşik kaynağı KULLANICIDAN alınmış (note okunup doğrulatılmış), uydurulmamış. validate-tech.mjs 0 error (checkRequiredRuleCoverage kapsam-warning'i üç ad için de DÜŞMÜŞ); emit-manifest.mjs çıktısında op kayıtlarında realizesRules: {rule, predicate}[] — üç op'ta da predicate DOLU (RealizePredNode: exists/count/and/or/cmp düğümleri; gövdesiz olsaydı null olurdu), meta.hasErrors:false.",
      "files": []
    }
  ]
}
